Edit the config/config.yaml file according to your choice (paths, ptbins, variables, training configurations)
Edit the BDT hyperparameters for each pt bin in config/xgbparams.yaml 

python3 run.py (for ONLY BDT Training Testing Performance and Histograms but NO FITS)

OR

./runanalysis (FOR FULL ANALYSIS)
